# Divineechidume.github.io

This repository holds the code for my personal website (GitHub Pages).

## Site

- **Entry point**: `index.html`
- **Styles**: `assets/styles.css`
- **JS (optional, minimal)**: `assets/main.js`

## Edit the content

- Update the hero copy and section text directly in `index.html`.
- Update the Writing list inside `assets/main.js` (`WRITING` array).
- Replace placeholder links (email + socials) in the `#contact` section.

## Local preview

Open `index.html` directly in your browser, or serve the folder with any static server.
